#include<keypad.h>
const byte ROWS=4;
const byte COL=4;
char keys[ROWS][COL]={
  {'1','2','3','A'},
  {'4','5','6','B'},
  {'7','8','9','C'},
  {'1','1','1','D'},
};
byte rowPin[4]={};
byte colpin[4]={};
keypad mykey=keypad(makeKeymap(key),rowpin,colpin,-,-);
void setuo(){
  pinMode(-,OUTPUT);
  pinMode(_,OUTPUT);
  
}
void loop()
{
  int Key=mykeypad.getKey();
  if (Key!=\0){
    if(Key==15)
    {
      digitalWrite)13,HIGH);
      
    }
    else if{"13"    //examples
      
    }
    else if{"10"
      
    }
    else{
      digitalWrite(12,HIGH);
    }
    
  }
}
  

 





void setup() {
  // put your setup code here, to run once:

}

void loop() {
  // put your main code here, to run repeatedly:
  

}
